# 任务清单: joy3d-modal-fix

## 1. 弹窗比例修复
- [√] 1.1 `ThreeEditorModal` 的 iframe 编辑器区域严格 1:1（扣除头/底栏），并以弹窗高度为标准自适配窗口
- [√] 1.2 监听 `resize`，弹窗尺寸随屏幕变化更新

## 2. 关闭后历史保留加固
- [√] 2.1 关闭 3D 弹窗时主动 `persistState(...)`，避免聊天记录本地持久化被意外覆盖/清空
- [√] 2.2 弹窗内控件（标题/按钮/输入/预览）尺寸随弹窗动态自适应（非固定像素）

## 3. 构建产物同步
- [√] 3.1 执行 `npm run export:unix`，更新 `frontend_dist/` 静态导出产物
